{
  "name": "reglet-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build:binaries": "bash scripts/build-binaries.sh",
    "build:macos-installer": "bash scripts/build-macos-installer.sh",
    "build:homebrew-cask": "bash scripts/generate-homebrew-cask.sh",
    "build:homebrew-formula": "bash scripts/generate-homebrew-formula.sh",
    "typecheck": "tsc --build",
    "lint": "eslint .",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0"
  }
}
