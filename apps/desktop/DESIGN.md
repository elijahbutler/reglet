# Desktop design authority

The shared manager design contract is defined in [`../../DESIGN.md`](../../DESIGN.md).

The Tauri client may document native window behavior, menus, updater flows,
deep links, reveal/open actions, and platform accessibility details here. It
must not redefine the shared manager's color, density, hierarchy, typography,
iconography, motion, or component shape.
